---
- hosts: 172.31.32.129 
  become: yes
  tasks:
      - include_tasks: myfile.yml
      
      - name: i want to install apache2 
        apt:
            name: apache2
            state: present 

